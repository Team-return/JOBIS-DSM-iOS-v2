# Production bundle ID (default)
app_identifier("com.team.return.JOBIS-DSM-iOS")
apple_id(ENV["APPLE_ID"])
team_id(ENV["TEAM_ID"])

# All bundle IDs for the app (dev, stage, prod)
for_platform :ios do
  # Production
  app_identifier("com.team.return.JOBIS-DSM-iOS")

  # Staging
  # app_identifier("com.team.return.JOBIS-DSM-iOS.stage")

  # Development
  # app_identifier("com.team.return.JOBIS-DSM-iOS.dev")
end
